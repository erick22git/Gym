node_modules/
dist/
dist-electron/
build/
!build/icon.ico
dev_data/
.env
.env.local
*.db
*.sqlite
electron/*.jsc

# Fondos de video (frames JPG) — pesados (~300MB), no hacen falta para
# clonar el proyecto y probar el pipeline de IA en otra máquina.
public/frames/

# Logs/snapshots de sesiones de testing con Playwright MCP — debug, no código.
.playwright-mcp/

# Servicio de OCR (Python): entorno virtual y caché — se recrean con
# scripts/requirements.txt, no van al repo.
venv-ocr/
__pycache__/
# Capturas y fotos de prueba (pesadas, con datos reales) — solo locales.
scripts/test_images/

# Recursos que se empaquetan dentro del instalador (binarios/modelos grandes, se regeneran con scripts/preparar-recursos)
resources-build/

# Artefacto generado por PyInstaller (se recrea con scripts/empaquetado/preparar-recursos.ps1)
ocr-service.spec

# Cualquier variante de .env (secretos)
.env.*
!.env.example
